spec:
  name: nis-preview
  static_sites:
    - name: nis-website
      git:
        branch: main
        repo_clone_url: https://github.com/telenexus-admin/neema.git
      source_dir: /
      index_document: index.html
